{%extends '::base.html.twig'%}
{% block stylesheets %}
    <style>
        body {
            padding-top: 60px;
            padding-bottom: 40px;        
        }            
    </style>
    <link href="{{asset('bundles/frontend/css/bootstrap-responsive.css')}}" type="text/css" rel="stylesheet"/>  
    <link href="{{asset('bundles/frontend/css/my-style.css')}}" type="text/css" rel="stylesheet"/>  
{% endblock %}
{%block body%}
    <div class="navbar navbar-inverse navbar-fixed-top">
            <div class="navbar-inner">
                <div class="container">
                    <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="brand" href="{{path('homepage')}}">Anuncios</a>
                    <div class="nav-collapse collapse">                        
                        <ul class="nav">                            
                            <li {%block minicio ''%}><a href="{{path('homepage')}}">Inicio</a></li>
                            <li {%block multimos ''%}><a href="{{path('anuncios_ultimos')}}">Ultimos anuncios</a></li>                            
                        </ul>
                        {%include 'FrontendBundle:Otros:cajaLogin.html.twig'%}
                    </div>
                </div>
            </div>
        </div>
        <div class="container">
            <div class="row-fluid">
                <div class="span3">                    
                    {{ render (controller('FrontendBundle:Anuncios:menuCategoria'))}}
                </div>
                <div class="span9">
                    {%block contenido%}
                    {%endblock%}
                </div>
            </div>
        </div>
{%endblock%}
